'use strict';

var app = angular.module('sr', ['leaflet-directive']);
app.value('bookgroups', bookgroups);
//angular.module('sr.controllers', []);

// app.run(function($rootScope, USER){
//     $rootScope.USER = USER;
// });